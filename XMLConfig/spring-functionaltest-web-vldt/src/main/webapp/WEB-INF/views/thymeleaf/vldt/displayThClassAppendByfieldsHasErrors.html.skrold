<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
  th:replace="~{thymeleaf/layout/template :: layout(~{::head/content()},~{::body/content()})}">
<head>
</head>
<body>
  <div class="container">
    <div id="wrapper">

      <h1 id="screenTitle">th:classappend="${#fields.hasErrors('fieldName')} ? 'error-label'"によるスタイル変更</h1>

      <form th:action="@{/vldt/0101/013}" method="post" th:object="${displayErrMessageForm}">
        <ul id="displayErrMessageForm.errors" th:if="${#fields.hasErrors('*')}">
          <li th:each="error : ${#fields.errors('*')}" th:text="${error}"></li>
        </ul>
        <label for="name" id="labelName" th:classappend="${#fields.hasErrors('name')} ? 'error-label'">Name:</label>
        <input type="text" th:field="*{name}" th:errorclass="error-input" />
        <br>
        <label for="email" id="labelEmail" th:classappend="${#fields.hasErrors('email')} ? 'error-label'">Email:</label>
        <input type="text" th:field="*{email}" th:errorclass="error-input" />
        <br>
        <label for="age" id="labelAge" th:classappend="${#fields.hasErrors('age')} ? 'error-label'">Age:</label>
        <input type="text" th:field="*{age}" th:errorclass="error-input" />
        <br>
        <button id="validate" name="validate" type="submit" value="validate">validate</button>
      </form>

    </div>
  </div>
</body>
</html>
